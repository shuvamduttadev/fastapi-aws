from fastapi import APIRouter

# from app.api.v2.endpoints import <new_endpoints>

api_router = APIRouter()
# api_router.include_router(<new_endpoints>.router, prefix="/<new_endpoints>", tags=["<new_endpoints>"])
